#ifndef HEADER_NAME_ONLY_HERE_H
#define HEADER_NAME_ONLY_HERE_H

/* No other search path directory holds this relative path, so it keeps a
   bracket name whatever the -I order is. */
struct header_name_only_here { int x; };

#endif
