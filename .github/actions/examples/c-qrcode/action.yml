name: c-qrcode Example
description: Build and run the c-qrcode example

runs:
  using: composite
  steps:
    - name: Run generation and build script
      shell: bash
      working-directory: ./examples/c-qrcode
      run: ./generate-and-run.sh
